apiVersion: v1
kind: PersistentVolume
metadata:
  name: ollama-pv
  namespace: bankapp-namespace
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 8Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/data/ollama"

