apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: bankapp-namespace
  name: ollama-pvc
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi

